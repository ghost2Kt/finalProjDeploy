# Copy to .env.local (gitignored) when using the mobile "Railway bridge" on your PC.
# Get the MySQL URL from Railway → MySQL → Connect → Public Network (mysql://root:...@host:port/railway)

DATABASE_URL="mysql://root:YOUR_PASSWORD@kodama.proxy.rlwy.net:37071/railway?serverVersion=8.0&charset=utf8mb4"
